<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testapplication">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApplication">
        <activity android:name=".camera.CameraMainctivity"/>
        <activity android:name=".camera.camera2.Camera2Activity" />
        <activity android:name=".touchtest.TouchTestMainActivity" />
        <activity android:name=".touchtest.ScrollTestInnerActivity" />
        <activity android:name=".touchtest.TouchTestActivity" />
        <activity android:name=".touchtest.ScrollTestOutActivity" />
        <activity android:name=".mvpapplication.LoginMvpActivity"/>
        <activity android:name=".mvpapplication.LoginSuccessActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".camera.camera1.CameraActivity" />
        <activity android:name=".mvvm.MvvmActivity" />
    </application>

</manifest>